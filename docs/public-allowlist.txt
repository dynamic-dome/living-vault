# Phase-11 Public-Vault Allowlist
# ================================
# Eine Wiki-relpath pro Zeile (relativ zu ~/wiki).
# # = Kommentar, Leerzeilen erlaubt.
#
# Jeder Pfad in dieser Datei wird beim public-build berücksichtigt,
# auch wenn die Page kein `public: true` im Frontmatter hat.
# Pfade die nicht in der DB existieren, werden im manifest.allowlist_skipped
# protokolliert und sind kein Build-Fehler.
#
# Workflow:
#   1. Pfad hinzufügen
#   2. ./scripts/deploy-public-vault.ps1
#   3. manifest.json prüfen
#   4. out-vault/index.html lokal im Browser öffnen
#
# Beispiel-Pfade (auskommentiert — User füllt selbst):
#
# concepts/a2a-protokoll.md
# concepts/mcp-gateway.md
# sources/mcp-oekosystem/index.md
# synthesis/2026-05-08-mcp-ideen-genese-notebooklm.md
